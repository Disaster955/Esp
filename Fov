-- FOV.lua
local RunService = game:GetService("RunService")
local fovEnabled = false
getgenv().fovValue = 70 -- valor global que se actualizará desde el slider
local fovConnection

-- Forzar FOV en cada frame
fovConnection = RunService.RenderStepped:Connect(function()
    if fovEnabled and workspace.CurrentCamera then
        workspace.CurrentCamera.FieldOfView = getgenv().fovValue
    end
end)

-- Función global para habilitar y cambiar FOV
getgenv().EnableFOV = function(value)
    fovEnabled = true
    getgenv().fovValue = value or 70
end

-- Función global para deshabilitar FOV
getgenv().DisableFOV = function()
    fovEnabled = false
end

-- Función de limpieza para unloadRemote
return function()
    getgenv().DisableFOV()
end
